# Simple Example

1. Move `templates/` directory into `backpack/`. Templates are included at compile-time.
1. Move `config.toml` to your root directory. Config is read at run-time on the command-line.
